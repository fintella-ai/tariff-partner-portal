"use client";
import Link from "next/link";
import { Sparkles } from "lucide-react";
interface Props { template: { id: string; name: string; status: string; updatedAt: string; variableKeys?: string[]; aiGenerated?: boolean; subject?: string; }; type: "email" | "sms"; }
export default function TemplateCard({ template: t, type }: Props) {
  const sc = t.status === "active" ? "bg-green-100 dark:bg-green-900/40 text-green-700 dark:text-green-300" : t.status === "archived" ? "bg-zinc-100 dark:bg-zinc-800 text-zinc-500" : "bg-amber-100 dark:bg-amber-900/40 text-amber-700 dark:text-amber-300";
  return (<Link href={`/admin/templates/${t.id}?type=${type}`} className="block border border-zinc-200 dark:border-zinc-700 rounded-xl p-4 bg-white dark:bg-zinc-900 hover:border-blue-300 dark:hover:border-blue-700 transition-colors"><div className="flex items-start justify-between mb-2"><h3 className="text-sm font-semibold text-zinc-900 dark:text-zinc-100 line-clamp-1">{t.name}</h3><span className={`px-2 py-0.5 rounded-full text-[11px] font-medium shrink-0 ${sc}`}>{t.status}</span></div>{t.subject && <p className="text-xs text-zinc-500 line-clamp-1 mb-2">{t.subject}</p>}<div className="flex items-center gap-2 mt-2 text-[10px] text-zinc-400">{t.aiGenerated && <span className="flex items-center gap-1 px-1.5 py-0.5 rounded bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-300 font-medium"><Sparkles className="w-3 h-3" />AI</span>}{(t.variableKeys?.length||0)>0 && <span>{t.variableKeys!.length} vars</span>}<span className="ml-auto">{new Date(t.updatedAt).toLocaleDateString()}</span></div></Link>);
}

"use client";
// Stub — v1 placeholder. The existing SMS log lives inside EmailTemplatesTab's
// internal tab bar. Follow-up spec will build a dedicated SMS panel.
export default function SmsTab() {
  return (
    <div className="font-body text-sm text-[var(--app-text-muted)]">
      SMS panel coming soon. The existing SMS send-log is viewable under the
      Templates view for now.
    </div>
  );
}

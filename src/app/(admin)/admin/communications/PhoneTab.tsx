"use client";
// Stub — v1 placeholder. The existing phone call log lives inside
// EmailTemplatesTab's internal tab bar. Follow-up spec will build a dedicated
// phone panel.
export default function PhoneTab() {
  return (
    <div className="font-body text-sm text-[var(--app-text-muted)]">
      Phone panel coming soon. The existing call log is viewable under the
      Templates view for now.
    </div>
  );
}
